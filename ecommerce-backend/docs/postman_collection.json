SPECTACULAR_SETTINGS["COMPONENTS"] = {
    "securitySchemes": {
        "BearerAuth": {
            "type": "http",
            "scheme": "bearer",
            "bearerFormat": "JWT",
        }
    }
}

Authorization: Bearer <token>

